{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-client": "pnpm install --prefix client",
    "install-server": "pnpm install --prefix server",
    "install": "pnpm run install-client & pnpm run install-server",
    "client": "pnpm dev --prefix client",
    "server": "pnpm dev --prefix server",
    "watch": "pnpm run client & pnpm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
