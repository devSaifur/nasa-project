{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client": "cd client && pnpm run dev",
    "server": "cd server && pnpm run dev",
    "build": "pnpm run client && pnpm run server",
    "dev": "concurrently --kill-others-on-fail \"pnpm run client\" \"pnpm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "devSaifur",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongoose": "^8.0.3"
  }
}
